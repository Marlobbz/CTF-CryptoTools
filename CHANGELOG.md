# 更新日志

本文件记录 CTF密码编解码工具箱 的版本迭代历史。所有条目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 的格式，并按 [语义化版本](https://semver.org/lang/zh-CN/) 管理版本号。

## [1.0.1] - 2026-08-13

### 说明

本次版本针对 **UI 界面与交互系统** 进行全方面优化，整体遵循 [kill-ai-slop](https://killaislop.com) 设计标准，目标是移除机器生成的视觉冗余（slop），建立清晰、克制、可解释的视觉与交互体验。核心编码逻辑未改动，全部 570 个单元测试保持通过。

### 界面视觉优化（遵循 kill-ai-slop 标准）

- **移除装饰性 emoji**：删除顶部导航、操作按钮、侧边栏分类、页脚等处的全部装饰性 emoji（如锁、放大镜、火箭、钥匙等），改为纯文本或功能性符号（如交换按钮使用 `⇄`）。对应标准：emoji 应承载信息而非装饰。
- **收敛为单一强调色**：移除原先的「红 + 蓝 + 紫」多色按钮与悬停彩色光晕，全站统一为一个品牌强调色 `#e94560`。对应标准：一个强调色、一种声音。
- **中性深色基底**：将带有蓝紫偏色的背景（`#1a1a2e` / `#16213e` / `#0f3460`）替换为扁平的中性深色（`#0f1115` 系列），去除渐变与彩色光晕。对应标准：扁平背景 + 发丝分隔线，而非渐变制造「高级感」。
- **统一小圆角**：全站圆角收敛为一致的小半径（6px / 4px），去除搜索框 24px 胶囊圆角与分类标签 20px 胶囊。对应标准：全站统一小圆角，避免「max-radius + 胶囊」。
- **克制阴影**：移除按钮悬停时的彩色光晕阴影与超大阴影，仅保留无色、克制的 `0 1px 2px` 与 `0 4px 12px` 两档。对应标准：阴影应反映真实高度，且保持无色。
- **清晰的字体层级**：以字号（标题 20px / 正文 14px / 辅助 12px）与留白构建信息层级，移除 `text-transform: uppercase` 与 `letter-spacing` 等对大写 kicker 的依赖。对应标准：层级来自字号与留白，而非颜色与描边。
- **移除状态框「一色到底」**：状态栏由「边框 + 背景 + 文字」三处同色，改为中性表面 + 一个小圆点标识状态，错误/成功先以文字表达。对应标准：状态先用文字与权重表达，颜色只作辅助。
- **过渡只作用于颜色/边框**：将全局 `transition: all` 改为只过渡 `background-color` / `border-color` / `color`，并移除按钮按压缩放（`scale(0.97)`）。对应标准：过渡只作用于携带状态变化的属性。

### 交互系统优化

- **移除内联样式与硬编码颜色**：`app.js` 中原先散落在高级选项、历史记录、Toast 中的内联样式与硬编码色值（`#1a1a2e`、`#22c55e`、`#3b82f6` 等）全部迁移至统一的 CSS 类与设计令牌。
- **统一状态提示机制**：`setStatus` 与 `showToast` 改为基于 CSS 类（`success` / `error`）的状态切换，替代原先的硬编码颜色字符串，视觉反馈一致。
- **补充加载状态**：编码、解码、复制操作增加按钮级加载指示（禁用 + 旋转图标 + 文案），在大输入或异步复制时提供明确反馈。
- **自动检测可点击直达**：自动检测结果由原先「仅 Toast 文字提示」升级为可点击的建议列表，点击即可切换到对应工具。
- **补齐历史记录单条删除**：历史记录面板新增「删除」按钮，修复原先仅能清空全部、无法删除单条的功能缺口。
- **类名化面板显隐**：历史面板、遮罩、移动端侧边栏改为基于 `active` / `open` 类名控制显隐，替代内联 `style.display`。
- **无障碍增强**：补充 `aria-label`、`aria-expanded`、`role="status"` / `role="dialog"`、`aria-live` 等属性，为状态提示与交互控件提供更完整的语义。
- **错误提示优化**：错误信息统一以「操作 + 原因」的清晰文案呈现，并配合状态栏与 Toast 双通道反馈。

### 兼容性

- 桌面端主流浏览器（Chrome / Edge / Safari / Firefox）与 Electron 桌面环境均验证通过。
- 移动端（< 768px）采用单列布局 + 汉堡菜单 + 全屏历史面板，响应式样式同步更新。
- 全量 9 个测试套件、570 个用例全部通过，核心编解码功能无回归。

### 涉及文件

- `src/ui/styles.css`：设计令牌与全部样式重构
- `index.html`：结构、语义与无障碍属性重构
- `src/ui/app.js`：交互逻辑、反馈机制、自动检测、历史记录重构
- `src/core/index.js`：移除分类 emoji 图标
- `electron/main.js`：窗口背景色同步更新
- `package.json`：版本号升至 1.0.1

## [1.0.0] - 首个版本

- 37 种常见密码与编码格式的双向编解码
- Electron 桌面应用（macOS / Windows / Linux）
- 自动检测、高级选项、历史记录、一键复制与下载
- 9 个测试套件、570 个用例
